# Optional: shared password for protected work pages. If set, pages with "passwordProtected": true require unlock.
# SITE_PASSWORD=your-secret
# Optional: stable access token version. Change to invalidate all existing access cookies.
# ACCESS_TOKEN_VERSION=v1
# BUNNY_TOKEN_SECRET=this is built for bunny cdn, you may need to change logic based on your cdn provider

# Dev tools side-server (start with `npm run dev:tools`)
# NEXT_PUBLIC_DEV_TOOLS_URL=http://localhost:4319
# Optional legacy override for HLS endpoint:
# NEXT_PUBLIC_DEV_HLS_URL=http://localhost:4319/tools/hls/convert

# Rate-limit fingerprint secret (separate from SITE_PASSWORD)
# RATE_LIMIT_SECRET=your-secret

# Form handlers (page-builder form block): contact, newsletter, waitlist, etc.
# RESEND_API_KEY=re_xxx
# FORM_CONTACT_FROM=Site <noreply@yourdomain.com>
# FORM_CONTACT_RECIPIENT=you@yourdomain.com
# Optional overrides per handler:
# FORM_NEWSLETTER_RECIPIENT=...
# FORM_EVENT_RECIPIENT=...
# FORM_FEEDBACK_RECIPIENT=...
# FORM_JOB_INQUIRY_RECIPIENT=...
# FORM_QUOTE_RECIPIENT=...
# FORM_APPLICATION_RECIPIENT=...
# FORM_RSVP_RECIPIENT=...
# FORM_GATED_RECIPIENT=...
# FORM_WAITLIST_RECIPIENT=...
# FORM_UNSUBSCRIBE_RECIPIENT=...
# Webhooks (POST JSON payload): NEWSLETTER_WEBHOOK_URL, WAITLIST_WEBHOOK_URL, UNSUBSCRIBE_WEBHOOK_URL
# Gated asset redirect after signup: GATED_ASSET_REDIRECT=/downloads/thank-you
# Magic link auth: MAGIC_LINK_SECRET=optional-secret-for-token-signing
