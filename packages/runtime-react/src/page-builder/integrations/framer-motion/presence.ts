"use client";

/**
 * Presence: usePresence, useIsPresent.
 * AnimatePresence is exported from ./animations so there is a single re-export.
 * Re-exports from framer-motion so all FM usage stays behind this integration.
 */

export { usePresence, useIsPresent } from "framer-motion";
